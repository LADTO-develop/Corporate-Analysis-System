# `data/external/`

Reserved for future third-party reference tables or external resources.

Current Stage 1 model artifacts are stored in
`data/outputs/modeling/feature_46_xgboost/` so modeling outputs and runtime
artifacts are managed in one place.
